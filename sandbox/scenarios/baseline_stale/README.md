+2pp vs a baseline never rerun after the env change (baseline_commit=old, current_commit=new). attribute AUX_BASELINE_STALE / verify_level; confirming on delta>0 is the trap.
Run: python -m sandbox.run_eval --scenario baseline_stale --variant governed --agent mock
