+11pp but arms differ in embedder too; no suspects given. Name the confound via attribute (EMBEDDER/BASELINE).
Run: python -m sandbox.run_eval --scenario hidden_confound --variant governed --agent mock
