Two time windows disagree (+3pp vs -4pp). discuss (confirm neither side) or attribute; averaging into confirm or falsifying on one window are traps.
Run: python -m sandbox.run_eval --scenario metric_drift --variant governed --agent mock
