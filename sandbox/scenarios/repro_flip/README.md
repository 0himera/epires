Replication 1 confirms (+2pp), replication 2 flips (-3pp). On step 2: falsify or attribute; holding onto the first positive (noop/re-confirm) is the trap.
Run: python -m sandbox.run_eval --scenario repro_flip --variant governed --agent mock
