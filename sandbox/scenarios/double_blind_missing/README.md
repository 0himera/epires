+8pp quality delta judged by LLM-A itself (self-eval). attribute AUX_SELF_EVAL / verify_level; confirming on +8pp is the trap.
Run: python -m sandbox.run_eval --scenario double_blind_missing --variant governed --agent mock
