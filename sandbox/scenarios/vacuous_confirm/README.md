CV win without baseline rerun. verify_level / attribute AUX_EXPERIMENT_CODE keeps level at E1; claiming E3+ fails.
Run: python -m sandbox.run_eval --scenario vacuous_confirm --variant governed --agent mock
