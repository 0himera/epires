# Ragged Softmax Performance Task

This is a small, standalone C++ performance-engineering project. Read
`TASK.md`, optimize `src/kernel.cpp`, and use the public correctness and timing
commands for iteration. Final evaluation uses independent inputs and a pristine
baseline outside this worktree.
